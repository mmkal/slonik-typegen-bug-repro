{
  "dependencies": {
    "@slonik/typegen": "0.12.1",
    "slonik": "28.0.0"
  },
  "devDependencies": {
    "prettier": "2.6.1",
    "ts-node": "10.7.0",
    "typescript": "4.6.3"
  },
  "scripts": {
    "typegen": "npx slonik-typegen generate --root-dir sql/ --connection-uri \"postgresql://postgres:postgres@localhost:26257/postgres?sslmode=disable\" --skip-check-clean --psql \"docker-compose exec -T postgres psql -h localhost -U postgres postgres\""
  }
}
